<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.upv.movie.list.netflix">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".PerfilActivity" android:label="@string/label_activity_perfil"></activity>

        <activity android:name=".ShowEditMovieActivity" />

        <activity android:name=".Ratings" />

        <activity android:name=".MovieListActivity"></activity>

        <activity android:name=".ListasActivity" android:theme="@style/AppThemeListas" />

        <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity android:name=".activity.InicioSesionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".InicioSesionActivity"></activity>
        <activity android:name=".RegistroActivity"></activity>
    </application>
</manifest>